options('redis:num'=TRUE)
require(doRedis)

library(base)
library(stats)



.First <- function(){
library(doRedis)
redisWorker(host='redis',queue='jobs',loglevel=1)
print("DONE")
}
# This needs to end on an empty line!!!!!!



